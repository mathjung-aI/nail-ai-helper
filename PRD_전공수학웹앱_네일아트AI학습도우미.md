# PRD — 전공·수학 웹앱 (네일아트 AI 학습 도우미 챗봇)

> \*\*Cursor 사용법\*\*: 이 문서 전체를 프로젝트 루트에 `PRD.md`로 저장한 뒤, Cursor 채팅창에
> `@PRD.md 이 PRD대로 Next.js 프로젝트를 처음부터 구현해줘. 8장 개발 순서대로 단계별로 진행하고, 각 단계가 끝나면 확인받아줘.`
> 라고 입력하세요. 부록 A\~D는 \*\*그대로 코드 파일로 옮겨 넣을 시드 데이터\*\*입니다.

\---

## 1\. 개요

### 1.1 제품 한 줄 정의

2026학년도 미디어메이크업과 2학년 「화가의 작품을 담은 메이크업쇼 네일아트」 3차시 융합수업에서, 학생이 **실습 중 떠오르는 질문을 스스로 해결**하고 **자신의 네일아트 디자인을 피드백받는** AI 학습 도우미 웹앱.

### 1.2 배경

* 본 수업은 **전공(네일 미용) 「입체 네일아트 › 젤 입체 네일아트 작업」이 메인**이고, **수학(경우의 수)은 디자인 의사결정을 뒷받침하는 협력·보조 도구**다.
* 3차시 구성: (1차시) 개념·화가 분석·AI 초안 → (2차시, 연구수업) 디자인 확정·젤 2D 조형·경화 → (3차시) 톱 젤 마무리·발표·성찰.
* 학습지도안 「매체 활용 계획」에 이 웹앱이 명시되어 있음: *"실습 중 떠오르는 전공 지식·전공연계 수학·수업 진행 질문에 학생이 스스로 질문·확인"*, *"디자인의 정답을 확정해 주지 않고 점검 질문(두께·위치·통일감)으로 되묻도록 설계"*.

### 1.3 핵심 설계 원칙 (반드시 지킬 것)

|#|원칙|구현상 의미|
|-|-|-|
|P1|**전공이 메인, 수학은 조력**|기본 답변 톤·예시·비유는 네일 실습 맥락. 수학은 "디자인 결정 근거"로 연결해서 설명|
|P2|**수학은 반드시 정확**|LLM 암산 금지. 계산은 **서버의 결정론적 계산기**가 수행하고, LLM은 식 세우기·해석만 담당|
|P3|**디자인은 정답을 주지 않는다**|디자인 피드백은 `잘된 점 → 보완점 → 점검 질문` 구조. "이게 정답"이라고 단정 금지|
|P4|**PDF를 런타임에 첨부하지 않는다**|NCS 교재 p.29\~37, 44\~47 내용을 **사전에 구조화한 로컬 지식베이스(TS 파일)** 로 내장|
|P5|**단순하게**|DB·벡터DB·로그인 없음. 키워드 기반 검색 + 파일 상수 + 브라우저 저장으로 충분|
|P6|**키는 서버에만**|모든 OpenAI 호출은 Next.js Route Handler(서버)에서. 클라이언트에 키 노출 절대 금지|

### 1.4 범위 (Scope)

**In scope**

* 채팅 기반 Q\&A (전공 실습 / 경우의 수 / 수업 진행 3개 도메인)
* 경우의 수 결정론적 계산기 + 풀이 설명
* 학생 디자인 이미지 업로드 → 루브릭 기반 피드백
* 추천 디자인(텍스트 명세 + AI 생성 이미지)
* 샘플 디자인 선제 제공 (학생 요청 시)
* 학습지도안 기반 추천 질문 칩(차시별/카테고리별)
* 조·이름 선택 및 질문 이력 로컬 저장 + 교사용 내보내기

**Out of scope**

* 회원가입/로그인, 서버 DB, 실시간 협업, 성적 산출, 결제
* 벡터 임베딩 RAG 파이프라인 (P4·P5에 따라 불필요)
* 모바일 네이티브 앱 (반응형 웹으로 디벗(아이패드) 대응)

\---

## 2\. 사용자 \& 시나리오

### 2.1 페르소나

|사용자|상황|필요|
|-|-|-|
|**학생 (주 사용자)**|특성화고 2학년, 아이패드(디벗) 사용, 손에 젤·장갑 → **터치 몇 번으로 끝나야 함**|큰 버튼, 추천질문 칩, 짧은 답변|
|**조장 (5명)**|별도 생성형 AI 계정 보유. 웹앱은 **검증** 용도|조합 수 확인, 제작 가능성 점검|
|**교사 (박기연)**|순회 지도 중|학생 질문 이력 훑어보기|

### 2.2 대표 시나리오

1. **(1차시)** 조장이 "베이스 컬러 5, 아트 기법 4, 입체 모티브 3이면 팁 디자인은 몇 가지?" → 웹앱이 곱의 법칙으로 `5×4×3 = 60가지` + 왜 곱셈인지 설명.
2. **(2차시)** 학생이 "이 두께로 조형해도 경화가 잘 될까요?" → NCS 지식베이스 기반으로 얇게 나눠 반복 경화, 핀 큐어 활용 안내.
3. **(2차시)** 학생이 자기 팁 사진을 찍어 업로드 → 5개 루브릭(제작 가능성/두께/위치/색 조화/통일감) 피드백 + 점검 질문 3개.
4. **(2차시)** "샘플 디자인 보여주세요" → 조 배정 화가에 맞춘 샘플 3안(텍스트 명세) 제시, 원하면 이미지 생성.
5. **(3차시)** "순서가 중요하지 않으면 순열 대신 뭘 써요?" → 조합 개념 + `10P3` vs `10C3` 비교 계산.

\---

## 3\. 기술 스택 \& 프로젝트 구조

### 3.1 스택

* **Next.js 15 (App Router) + TypeScript**
* **Tailwind CSS** (별도 UI 라이브러리 없이, lucide-react 아이콘만)
* **OpenAI Node SDK** (`openai` v4)
* 상태: React `useState` / `useReducer` (전역 상태관리 라이브러리 없음)
* 저장: `localStorage` (조/이름/대화 이력)
* 배포: Vercel (환경변수는 Vercel Dashboard에 등록)

### 3.2 모델 선택

|용도|모델 (env로 교체 가능)|
|-|-|
|일반 채팅 / 수학 설명|`gpt-4.1-mini` (기본)|
|이미지(디자인) 피드백|`gpt-4.1` (vision)|
|디자인 이미지 생성|`gpt-image-1`|

> 모델명은 반드시 `process.env.OPENAI\_CHAT\_MODEL` 등으로 주입. 하드코딩 금지 (모델은 교체될 수 있음).

### 3.3 폴더 구조

```
/app
  /page.tsx                     # 메인 채팅 화면
  /teacher/page.tsx             # 교사용 질문 이력 뷰 (선택)
  /api/chat/route.ts            # 채팅 (스트리밍)
  /api/counting/route.ts        # 경우의 수 계산 (결정론적, LLM 미사용)
  /api/design-feedback/route.ts # 이미지 업로드 → 피드백
  /api/design-generate/route.ts # 추천/샘플 디자인 이미지 생성
  /layout.tsx, /globals.css
/components
  ChatWindow.tsx  MessageBubble.tsx  Composer.tsx
  SuggestedQuestions.tsx          # 추천 질문 칩
  GroupSelector.tsx               # 조/화가/이름 선택 온보딩
  DesignUploader.tsx              # 사진 업로드 + 피드백 카드
  DesignGallery.tsx               # 샘플/추천 디자인 카드
  CountingResultCard.tsx          # 계산 결과 강조 카드
  ModeTabs.tsx                    # 전공 / 경우의 수 / 수업안내
/lib
  openai.ts                       # 클라이언트 초기화 + MOCK 모드 분기
  prompts.ts                      # 시스템 프롬프트 전문 (9장)
  counting.ts                     # nPr, nCr, 팩토리얼, 식 파서
  retrieve.ts                     # 키워드 기반 지식 검색
  types.ts
/lib/knowledge
  nail.ts        # 부록 A — NCS p.29\~37, 44\~47
  math.ts        # 부록 B — 2022 개정 공통수학1 경우의 수
  lesson.ts      # 부록 C — 학습지도안 요약 + 추천질문
  artists.ts     # 부록 D — 조별 화가 배정 + 디자인 요소 팔레트
/public
  /samples/\*.png                  # 사전 생성해 둔 샘플 디자인 이미지(폴백)
```

\---

## 4\. 환경변수 (가상 키 → 추후 실제 키로 교체)

`.env.local` (그리고 `.env.example`도 같이 생성):

```bash
# ─────────── OpenAI ───────────
# ⚠️ 아래는 실제 동작하지 않는 가상 키입니다.
#    배포 전 실제 키로 교체하세요. (교체 지점: 이 파일 1곳뿐)
OPENAI\_API\_KEY=sk-proj-YbkA8OcnRmDKd45lrU3bi\_UVetY-fbm8okTeJKZn4wG3-NowfcgFC9IxxWJuZ1icn\_zBLbBDs5T3BlbkFJlCSsXvBwwr3H5IZvMJZczeEcnmNw3-nNH7X\_6FGNc8Z3YtFyqIbz63cTumZsUF4o1HgS0JWrgA

OPENAI\_CHAT\_MODEL=gpt-4.1-mini
OPENAI\_VISION\_MODEL=gpt-4.1
OPENAI\_IMAGE\_MODEL=gpt-image-1

# ─────────── 앱 설정 ───────────
# true 이면 OpenAI를 호출하지 않고 lib/mock/\*.ts 의 고정 응답을 반환한다.
# 실제 키 없이 UI/UX 전체를 개발·시연할 수 있게 하는 스위치.
MOCK\_MODE=true

# 교사 페이지 접근 비밀번호 (선택 기능)
TEACHER\_PASSCODE=1234
```

**필수 구현 사항**

1. `lib/openai.ts`에서 `MOCK\_MODE === 'true'`이면 OpenAI SDK를 아예 호출하지 않고 목 응답을 반환한다. (스트리밍도 목으로 흉내낼 것)
2. `OPENAI\_API\_KEY`가 `DUMMY`로 시작하면 서버 부팅 시 콘솔에 경고: `⚠️ 가상 키 사용 중 — MOCK\_MODE로 동작합니다.`
3. `.env.local`은 `.gitignore`에 포함.

\---

## 5\. 기능 요구사항

### F-01. 온보딩 (조·이름 선택)

* 첫 진입 시 모달: **조 선택(1\~5조)** → 조를 고르면 배정 화가가 자동 표시(부록 D). 이름 입력(선택, 미입력 시 "익명").
* `localStorage.setItem('nailapp.profile', ...)`. 이후 재진입 시 건너뜀. 상단바에서 언제든 변경 가능.
* 선택된 조/화가는 **모든 API 호출의 컨텍스트**로 전달되어, 답변·샘플 디자인이 해당 화가 톤으로 나온다.

### F-02. 채팅 (3개 모드)

상단 탭으로 모드 전환. 모드에 따라 **시스템 프롬프트와 지식 소스가 달라진다.**

|모드|지식 소스|답변 스타일|
|-|-|-|
|🎨 **전공 실습** (기본)|`nail.ts`|실습 절차 중심, 안전·위생 수칙 자동 첨부|
|🔢 **경우의 수**|`math.ts` + `/api/counting`|식 → 계산 → 해석 → 디자인 연결|
|📋 **수업 안내**|`lesson.ts`|차시별 활동·평가기준·준비물 안내|

* **스트리밍 응답** 필수 (Route Handler에서 `ReadableStream` 반환).
* 답변 길이: 기본 **3\~6문장 + 필요 시 3\~5개 불릿**. 실습 중 읽는 화면이므로 길면 안 됨.
* 답변 하단에 **출처 배지**: `📖 NCS 입체 네일아트 p.31` / `📐 공통수학1 경우의 수` / `📋 학습지도안 2차시`.
* 대화 이력은 최근 10턴만 컨텍스트로 전송(토큰 절약).

### F-03. 지식 검색 (RAG 대체 — 키워드 기반)

`lib/retrieve.ts`:

```ts
export function retrieve(query: string, domain: Domain, k = 3): KnowledgeChunk\[]
```

* 각 지식 청크에 `keywords: string\[]`를 미리 부여(부록 A/B/C 참조).
* 점수 = (쿼리에 포함된 키워드 수 × 3) + (제목 부분일치 × 2) + (본문 부분일치 × 1).
* 상위 k개를 시스템 프롬프트의 `<참고자료>` 블록에 삽입.
* **매칭 청크가 0개면** LLM에게 "교재에 해당 내용이 없으니 일반 지식으로 답하되 그 사실을 밝히라"고 지시.

### F-04. 경우의 수 계산기 (정확성 보장 — 최우선 기능)

**문제**: LLM은 `8C5` 같은 계산을 틀릴 수 있음.
**해결**: 2단계 구조.

1. LLM이 **툴 콜(function calling)** `calculate\_counting`을 호출하며 구조화된 식만 만든다.

```ts
// tool schema
{
  name: "calculate\_counting",
  parameters: {
    steps: \[{
      op: "permutation" | "combination" | "product" | "sum" | "factorial" | "power",
      n?: number, r?: number, values?: number\[],
      label: string   // 예: "베이스 컬러 선택"
    }],
    combine: "product" | "sum" | "single"
  }
}
```

2. 서버가 `lib/counting.ts`로 **정확히 계산**하고, 결과를 LLM에 되돌려 자연어 해설을 생성.

`lib/counting.ts` 요구사항:

* `factorial(n)`, `permutation(n,r)`, `combination(n,r)` — **BigInt** 사용 (오버플로 방지), 최종 표시는 문자열.
* 유효성 검사: `n, r` 은 0 이상 정수, `r ≤ n`. 위반 시 명확한 한국어 에러 메시지.
* `n > 170` 은 거부하고 "수업 범위를 벗어난 값"이라 안내.
* 단위 테스트 필수: `8C5=56`, `10P3=720`, `6P4=360`, `5×4×3=60`, `0!=1`, `nC0=1`, `nCr=nC(n-r)`.

**UI**: 계산이 포함된 답변은 `CountingResultCard`로 렌더 —

```
┌──────────────────────────────┐
│ 8C5 = 56 가지                 │
│ 후보 모티브 8개 중 5개 선택    │
│ ▸ 순서 상관없음 → 조합(C)     │
│ ▸ 8C5 = 8!/(5!·3!) = 56      │
└──────────────────────────────┘
```

### F-05. 디자인 피드백 (사진 업로드)

* 학생이 제작 중/완성 팁을 **카메라 촬영 또는 파일 업로드** (최대 4MB, jpg/png/webp, 자동 리사이즈 1024px).
* Vision 모델에 **5개 루브릭**(2차시 활동1 교사 순회 질문 그대로)으로 평가 요청:

  1. **제작 가능성** — 젤로 실제 조형 가능한 형태인가
  2. **두께** — 입체 장식이 과도하게 두껍지 않은가 (경화 불량/생활 불편)
  3. **위치** — 팁 끝부분에 몰려 있지 않은가
  4. **색 조화** — 베이스 컬러와 장식 색이 어울리는가
  5. **컬렉션 통일감** — 배정 화가의 분위기가 10팁 전체에 드러나는가
* **출력은 JSON 강제** (프롬프트에 "JSON만, 마크다운 백틱 없이" 명시) 후 파싱:

```ts
{
  overall: string,              // 1\~2문장 총평
  rubric: \[{ name, level: "좋음"|"보통"|"보완필요", comment }],
  strengths: string\[],          // 2\~3개
  improvements: string\[],       // 2\~3개, 구체적 행동 지시
  checkQuestions: string\[],     // 3개 — ★정답 대신 되묻는 질문 (P3)
  safetyNotes: string\[]         // 해당될 때만
}
```

* `checkQuestions`는 반드시 포함 — **정답을 확정해 주지 않는다는 수업 설계 원칙의 핵심**.
* 파싱 실패 시 원문 텍스트로 폴백 렌더링.
* 이미지는 **서버에 저장하지 않음** (개인정보). 요청-응답 1회성.

### F-06. 추천 디자인 생성

* 입력: 조(화가), 선택한 디자인 요소(베이스 컬러 / 아트 기법 / 입체 모티브 — 부록 D 팔레트에서 칩으로 선택), 자유 텍스트.
* 출력 2단:

  1. **디자인 명세 카드**(텍스트, 채팅 모델): 컬렉션 콘셉트 1줄 / 10팁 배열 계획 / 팁별 요소 / 사용 젤·도구 / 제작 순서 요약 / **경우의 수 근거**(예: "5×4×3=60가지 중 통일감 기준으로 3안 압축")
  2. **이미지**(`gpt-image-1`, 1024×1024, 최대 3장) — 네일 팁 위 젤 2D 입체 아트의 클로즈업.
* 이미지 프롬프트 템플릿 (영문, 코드 상수):

```
Close-up product photograph of a set of {n} artificial nail tips arranged in a row on a clean white surface.
Each tip features hand-sculpted 2D embossed gel nail art. Base color: {baseColor}.
Motif: {motif}, rendered in a raised, glossy, semi-three-dimensional gel texture.
Art technique: {technique}. Overall mood inspired by {artistMood} — do NOT reproduce any existing artwork.
Soft studio lighting, shallow depth of field, high detail, no hands, no text, no watermark.
```

* **저작권 가드**: 프롬프트에 화가의 **작품 제목·직접 재현 요청을 넣지 않는다.** `artistMood`에는 색·선·질감 형용사만 사용(부록 D). 생존 작가(쿠사마 야요이)는 특히 **작가명 대신 요소 형용사만** 사용.

### F-07. 샘플 디자인 선제 제공

* 채팅 첫 화면 및 "샘플 보기" 버튼 → 조 배정 화가에 맞춘 **샘플 3안**을 즉시 카드로 표시.
* 각 조 × 3안 = 15개 샘플은 **`lib/knowledge/artists.ts`에 텍스트 명세로 사전 작성**해 두고(부록 D), 이미지는

  * `MOCK\_MODE=true` → `/public/samples/{group}-{n}.png` 폴백 이미지 사용
  * 실제 키 사용 시 → 최초 요청 때 생성 후 브라우저 캐시
* 샘플 카드 하단에 항상: **"이건 예시일 뿐입니다. 조에서 분석한 화가 요소로 바꿔 보세요."**

### F-08. 추천 질문 칩 (학습지도안 기반)

* Composer 위에 가로 스크롤 칩. **차시 선택(1/2/3차시) × 카테고리(전공/경우의수/수업진행)** 로 필터.
* 전체 목록은 부록 C. 칩 클릭 = 해당 텍스트로 즉시 전송.
* 현재 모드에 맞는 칩이 기본 노출되고, 대화 도중에는 **직전 답변 기반 후속 질문 3개**를 LLM이 함께 생성해 칩으로 표시(응답 JSON의 `followUps` 필드).

### F-09. 질문 이력 \& 교사 뷰 (P1, 선택 구현)

* 학생 브라우저 `localStorage`에 `{ts, group, name, mode, question, answerSummary}` 누적.
* 학생 화면: "내 질문 기록" 시트 + **"교사에게 제출" → JSON/CSV 다운로드**.
* `/teacher` 페이지: 패스코드 입력 후, 업로드된 JSON 파일들을 드래그\&드롭 → 조별·모드별 질문 빈도 요약 표시. (서버 저장 없음)

\---

## 6\. API 명세

### `POST /api/chat`

```ts
// req
{ mode: 'nail'|'math'|'lesson', messages: {role,content}\[], profile: {group:number, artist:string, name?:string} }
// res: text/event-stream (SSE)
// 최종 이벤트로 메타데이터 전송:
// data: {"type":"meta","sources":\["NCS p.31"],"followUps":\["...","...","..."],"counting":{...}|null}
```

### `POST /api/counting`

```ts
// req  { steps: Step\[], combine: 'product'|'sum'|'single' }
// res  { ok: true, total: string, breakdown: \[{label, expression, value}] }
//      | { ok: false, error: string }
```

> 이 라우트는 \*\*LLM을 호출하지 않는다.\*\* 순수 계산. 프론트에서 직접 호출도 가능(계산기 위젯).

### `POST /api/design-feedback`

```ts
// req  multipart/form-data: image(File), note?(string), profile(JSON string)
// res  { ok:true, feedback: DesignFeedback } | { ok:false, error }
```

### `POST /api/design-generate`

```ts
// req  { artist, baseColor, technique, motif, freeText?, count?:1|2|3, withImage:boolean }
// res  { spec: DesignSpec, images: string\[] }  // images = base64 data URL 또는 /samples 경로
```

**공통 에러 처리**: 모든 라우트는 실패 시 한국어 메시지 반환. 학생 화면에는 기술 용어 없이
`"지금은 답변을 가져오지 못했어요. 잠시 후 다시 시도하거나 선생님께 알려 주세요."` + 재시도 버튼.

\---

## 7\. 시스템 프롬프트 (lib/prompts.ts — 아래 문구 그대로 사용)

### 7.1 공통 헤더 (모든 모드 앞에 붙임)

```
당신은 광신방송예술고등학교 미디어메이크업과 2학년 「입체 네일아트 × 경우의 수」 융합수업의
학습 도우미 챗봇입니다. 사용자는 만 17세 전후의 특성화고 학생입니다.

\[대화 원칙]
- 존댓말, 따뜻하고 간결하게. 기본 3\~6문장. 필요할 때만 불릿 3\~5개.
- 이 수업의 중심은 '전공(네일 미용) 실습'입니다. 수학은 디자인 결정을 뒷받침하는 도구로만 다룹니다.
- 학생이 스스로 판단하도록 돕습니다. 디자인의 '정답'을 단정하지 말고, 판단 기준과 점검 질문을 제시하세요.
- 제공된 <참고자료> 범위 밖의 내용은 지어내지 말고 "교재에서 확인되지 않는 부분"이라고 밝히세요.
- 안전·위생 관련 질문에는 반드시 해당 수칙을 함께 안내하세요.
- 학생의 조: {group}조 / 배정 화가: {artist}

\[현재 조 정보]
{groupContext}
```

### 7.2 전공 실습 모드 (`nail`)

```
\[역할] 젤 입체 네일아트 실습 도우미.

\[답변 방식]
1) 학생 질문이 어떤 작업 단계(소독 → 베이스/컬러 → 조형 → 경화 → 디테일 → 톱 젤 마무리 → 접착)에
   해당하는지 먼저 짚어 주세요.
2) <참고자료>의 수행 순서와 수행 tip을 근거로 구체적으로 답하세요.
3) 젤 램프·미경화 젤·두께·환기 관련 내용이 조금이라도 걸리면 안전 수칙을 한 줄 덧붙이세요.
4) 답변 끝에 이 수업 맥락의 점검 질문을 1개 던지세요.
   (예: "10팁 전체로 봤을 때 이 두께가 통일감을 해치지는 않을까요?")

\[금지]
- 교재에 없는 제품명·브랜드 추천
- "이렇게 하는 게 정답입니다" 식 단정
- 의학적 조언(피부 트러블 등은 "선생님 또는 보건교사께 알리세요"로 안내)

<참고자료>
{chunks}
</참고자료>
```

### 7.3 경우의 수 모드 (`math`)

```
\[역할] 공통수학1 「경우의 수」를 네일 디자인 맥락에서 설명하는 도우미.

\[절대 규칙]
- 숫자 계산을 직접 하지 마세요. 곱셈·팩토리얼·nPr·nCr이 필요하면 반드시
  calculate\_counting 도구를 호출하고, 도구가 돌려준 값만 사용하세요.
- 도구 결과와 다른 숫자를 답변에 쓰면 안 됩니다.

\[답변 구조]
1) 상황 파악 — 무엇을 세는지 한 문장으로 다시 정리
2) 원리 선택 — 합/곱/순열/조합 중 무엇이고 왜 그것인지
   · "또는", 동시에 일어나지 않음 → 합의 법칙
   · "그리고", 단계적으로 이어짐 → 곱의 법칙
   · 뽑아서 '나열·배치·순서' → 순열 nPr
   · 뽑기만 하고 '순서 무관' → 조합 nCr
3) 식과 값 (도구 결과 인용)
4) 디자인 해석 — 이 수가 컬렉션 결정에 어떤 의미인지 1\~2문장
   (예: "60가지 중 화가의 분위기가 통일되는 3안으로 좁히는 것이 이번 과제입니다.")

\[수업 예시 값 — 학생이 헷갈려 하면 참고]
곱의 법칙 5×4×3=60 / 조합 8C5=56 / 순열 10P3=720 / 6P4=360

<참고자료>
{chunks}
</참고자료>
```

### 7.4 수업 안내 모드 (`lesson`)

```
\[역할] 3차시 수업 진행 안내자.
학습지도안에 적힌 범위(차시별 활동, 시간, 준비물, 평가기준, 발표 구조)만 안내하세요.
지도안에 없는 일정·점수·규칙은 "선생님께 확인이 필요해요"라고 답하세요.

<참고자료>
{chunks}
</참고자료>
```

### 7.5 디자인 피드백 프롬프트 (vision)

```
당신은 네일아트 실습 지도 보조입니다. 학생이 제작 중인 젤 2D 입체 네일아트 사진을 봅니다.
학생의 조 배정 화가는 {artist} 입니다.

아래 5개 루브릭으로 평가하되, '정답 디자인'을 제시하지 말고 학생이 스스로 고치도록 돕는 톤으로 쓰세요.
1) 제작 가능성  2) 입체 장식의 두께  3) 장식의 위치  4) 색 조화  5) 컬렉션 통일감

반드시 아래 JSON 스키마만 출력하세요. 마크다운 코드펜스, 설명문 없이 JSON 객체 하나만.
{ "overall": string, "rubric": \[{"name": string, "level": "좋음"|"보통"|"보완필요", "comment": string}],
  "strengths": string\[], "improvements": string\[], "checkQuestions": string\[], "safetyNotes": string\[] }

- comment는 각 1\~2문장, 한국어 존댓말.
- checkQuestions는 정확히 3개, 모두 물음표로 끝나는 되묻는 질문.
- 사진이 흐리거나 네일이 아닌 경우 overall에 그 사실을 적고 나머지는 빈 배열로 두세요.
```

\---

## 8\. UI/UX 요구사항

### 8.1 전반

* **대상 기기: 아이패드 가로/세로 + 교사 PC**. 반응형 필수. 터치 타깃 최소 44px.
* 실습실 조명 아래에서 보이도록 **명도 대비 충분히** (본문 텍스트 최소 16px, 색 대비 4.5:1 이상).
* 톤: 밝은 뉴트럴 배경 + 조별 화가 색을 액센트로 (부록 D의 `accentHex`).
* 로딩: 스켈레톤 + "생각하는 중…" 대신 맥락 문구 ("교재를 찾아보는 중…", "조합 수를 계산하는 중…").

### 8.2 화면 구성

```
┌─────────────────────────────────────────────────┐
│ 🎨 네일아트 AI 학습 도우미     \[3조 · 클림트 ▾]  │  상단바
├─────────────────────────────────────────────────┤
│ \[🎨 전공 실습] \[🔢 경우의 수] \[📋 수업 안내]     │  모드 탭
├─────────────────────────────────────────────────┤
│                                                  │
│   (대화 영역 — 말풍선, 출처 배지, 결과 카드)      │
│                                                  │
├─────────────────────────────────────────────────┤
│ \[1차시]\[2차시]\[3차시]  ← 추천질문 칩 가로 스크롤  │
│ ( 톱 젤은 언제 바르나요? )( 8C5는 왜 조합? ) ... │
├─────────────────────────────────────────────────┤
│ \[📷 내 디자인 피드백] \[✨ 샘플 디자인]  \[입력…] ▶ │  하단 액션바
└─────────────────────────────────────────────────┘
```

### 8.3 세부

* **말풍선**: 학생=우측 액센트색, AI=좌측 흰 카드. AI 답변에 `복사` 버튼(활동지 기록용).
* **출처 배지**: 답변 하단 작은 pill. 클릭 시 해당 지식 청크 원문을 하단 시트로 표시 → *학생이 교재 페이지를 직접 확인하도록 유도*.
* **디자인 피드백 카드**: 5개 루브릭을 신호등(초록/노랑/주황) 배지 + 코멘트. `점검 질문 3개`는 별도 강조 박스에 "생각해 볼 질문" 제목으로.
* **샘플/추천 디자인 카드**: 이미지 + 콘셉트 1줄 + 요소 태그 + "이 안으로 활동지에 기록하기(복사)" 버튼.
* **빈 화면(첫 진입)**: 조 배정 화가 소개 1문단 + 추천질문 6개 + 샘플 보기 버튼.

\---

## 9\. 개발 순서 (Cursor에 이 순서로 지시)

|단계|산출물|완료 기준|
|-|-|-|
|**S1**|Next.js + TS + Tailwind 초기화, `.env.local`/`.env.example`, `lib/openai.ts`(MOCK 분기)|`npm run dev` 기동, 홈에 "MOCK 모드" 배지 표시|
|**S2**|`lib/knowledge/\*.ts` 4개 파일 (부록 A\~D 내용 입력), `lib/retrieve.ts`|키워드 검색 유닛 테스트 통과|
|**S3**|`lib/counting.ts` + `/api/counting` + 유닛 테스트|8C5=56, 10P3=720, 6P4=360, 0!=1 통과|
|**S4**|`/api/chat` (스트리밍 + 툴 콜) + `lib/prompts.ts`|MOCK으로 3개 모드 답변 스트리밍 확인|
|**S5**|채팅 UI (ChatWindow/Composer/MessageBubble/ModeTabs) + 온보딩|조 선택 → 대화 → 이력 유지|
|**S6**|`SuggestedQuestions` + 출처 배지 + 결과 카드|칩 클릭으로 전송, 계산 카드 렌더|
|**S7**|`/api/design-feedback` + `DesignUploader`|사진 업로드 → 루브릭 카드 (MOCK 응답 포함)|
|**S8**|`/api/design-generate` + `DesignGallery` + `/public/samples` 폴백|조별 샘플 3안 즉시 표시|
|**S9**|질문 이력 저장/내보내기 + `/teacher`|JSON 다운로드 → 교사 페이지 업로드|
|**S10**|접근성·반응형 점검, 에러 폴백, README(키 교체 방법 1문단)|아이패드 폭 768/1024에서 레이아웃 정상|

\---

## 10\. 안전·윤리·저작권 가드레일 (필수 구현)

1. **저작권** — AI 이미지 생성 시 특정 작품의 재현을 요청하지 않는다. 색·선·질감·분위기 형용사만 사용. 생존 작가(쿠사마 야요이)는 작가명을 프롬프트에 넣지 않고 요소 형용사(물방울 반복 패턴, 대비 강한 원색 등)만 사용.
2. **NCS 자료 출처 표기** — 지식베이스 사용 시 답변에 `교육부(2025) NCS학습모듈 「입체 네일아트」 p.xx` 배지 표기. `README`와 앱 하단 푸터에도 출처 명시.
3. **개인정보** — 업로드 이미지·이름을 서버에 저장하지 않는다. 이름은 선택 입력, 실명 강요 금지.
4. **AI 의존 방지** — 답변마다 점검 질문 1개 이상. 디자인 피드백에는 반드시 `checkQuestions` 3개.
5. **안전 수칙 자동 첨부** — 젤 램프 광원 직시 금지 / 제품별 경화 시간 준수 / 미경화 젤은 젤 클렌저 처리 / 마스크·환기 / 장식 두께·위치 사전 점검.
6. **범위 밖 질문** — 수업과 무관한 요청(과제 대필, 사적 대화 등)은 정중히 수업 맥락으로 되돌린다.
7. **비용 가드** — 이미지 생성은 세션당 최대 6장, 채팅은 `max\_tokens` 700으로 제한.

\---

## 11\. 수용 기준 (Acceptance Criteria)

* \[ ] 실제 API 키 없이(`MOCK\_MODE=true`) 전 기능 시연 가능
* \[ ] `.env.local`의 `OPENAI\_API\_KEY` 한 줄만 바꾸면 실제 동작으로 전환
* \[ ] 브라우저 네트워크 탭·번들에 API 키 문자열이 전혀 노출되지 않음
* \[ ] `8C5`, `10P3`, `6P4`, `5×4×3` 질문에 각각 56 / 720 / 360 / 60 정확히 응답
* \[ ] "톱 젤은 언제 바르나요?" → NCS 마무리 단계 절차 + 출처 배지 표시
* \[ ] "미경화 젤은 어떻게 처리하나요?" → 젤 클렌저 안내 + 안전 수칙 첨부
* \[ ] 사진 업로드 → 5개 루브릭 + 점검 질문 3개가 항상 포함
* \[ ] 조 선택 시 해당 화가 샘플 3안이 3초 내 표시(MOCK 기준)
* \[ ] 추천 질문 칩이 차시별로 필터링되고 클릭 시 즉시 전송
* \[ ] 아이패드(768px, 1024px) 세로/가로에서 레이아웃 깨짐 없음
* \[ ] 어떤 API 오류에서도 앱이 크래시하지 않고 한국어 안내 + 재시도 버튼 표시

\---

# 부록 A. 네일 지식베이스 시드 (`lib/knowledge/nail.ts`)

> 출처: 교육부(2025). NCS학습모듈 「입체 네일아트」(LM1201010455\_23v4). 한국직업능력연구원. \*\*p.29\~37, 44\~47\*\*을 수업용으로 요약·구조화함.

```ts
export type Chunk = {
  id: string; title: string; page: string;
  keywords: string\[]; body: string; safety?: string\[]; tips?: string\[];
};

export const NAIL\_CHUNKS: Chunk\[] = \[
  {
    id: "n-01", title: "젤 입체 네일아트 개요 (2D / 3D)", page: "p.29",
    keywords: \["입체","2D","3D","젤","개념","반고형","볼륨","차이"],
    body: "입체 네일아트는 2D와 3D로 나뉜다. 2D는 젤이나 아크릴을 손톱 위에 직접 올려 만드는 반입체적 아트이고, 3D는 외부에서 따로 만들어 손톱에 붙이는 입체 장식이다. 젤은 반고형 상태라 볼륨을 주며 조형할 수 있고, 철사 같은 틀이나 다양한 응용 재료로 형태를 잡을 수도 있다."
  },
  {
    id: "n-02", title: "네일 몰드(실리콘 틀)", page: "p.29\~30",
    keywords: \["몰드","실리콘","틀","3D","반복","균일"],
    body: "네일 몰드는 3D 모형을 만들 때 쓰는 실리콘 틀이다. 균일하고 정교한 형태를 만들 수 있고 반복 사용이 가능해 경제적이다."
  },
  {
    id: "n-03", title: "튜브 젤", page: "p.29\~30",
    keywords: \["튜브 젤","튜브","연장","스톤","파츠","고정"],
    body: "튜브에 담긴 젤로, 제형에 따라 네일 연장에도 쓸 수 있다. 스톤이나 파츠를 고정하는 용도, 2D 아트 작업에도 사용한다."
  },
  {
    id: "n-04", title: "젤 2D 리본 — 재료·도구", page: "p.31",
    keywords: \["리본","재료","도구","준비물","브러시","보안경"],
    body: "재료: 소독제, 마스크, 네일 접착제, 경화 촉진제, 전 처리제, 베이스 젤, 클리어 젤, 컬러 젤, 톱 젤, 젤 클렌저, 알루미늄 포일, 오렌지 우드스틱. 기기·공구: 작업대와 의자, 자외선 소독기, 젤 램프 기기, 젤 브러시, 보안경."
  },
  {
    id: "n-05", title: "젤 2D 리본 — 수행 순서", page: "p.31\~33",
    keywords: \["리본","순서","조형","엠보젤","경화","스패출러","우드스틱"],
    body: "① 작업자와 고객의 손·손톱을 소독한다. ② 리본과 어울리는 젤 컬러를 도포한다. ③ 엠보 젤 전용 브러시로 리본 형태를 조형한다(우드스틱이나 스패출러를 쓰면 수월하다). ④ 형태가 잡히면 젤 램프로 경화한다. 작업 환경이 따뜻하면 젤이 흘러 형태가 흐려질 수 있으므로, 디자인에 따라 순차적으로 나누어 경화한다. 리본 한쪽을 완성한 뒤 짧게 경화하면 형태가 안정되어 다음 작업이 수월하다. ⑤ 적당량의 톱 젤을 도포·경화하여 마무리한다.",
    tips: \[
      "젤 클렌저로 브러시를 닦아 가며 작업하면 조형이 깔끔해진다.",
      "젤 볼이 퍼지는 것을 막으려면 단계마다 핀 큐어 램프로 형태를 고정한다."
    ],
    safety: \[
      "젤 램프의 광원을 눈으로 직접 보지 않는다.",
      "제품별 경화 시간을 지킨다.",
      "미경화 젤이 남으면 젤 클렌저로 닦아낸다.",
      "마스크를 착용하고 환기에 유의한다.",
      "입체 장식이 생활에 불편을 주지 않도록 디자인한다."
    ]
  },
  {
    id: "n-06", title: "젤 2D 케이크 — 수행 순서", page: "p.34\~37",
    keywords: \["케이크","튜브 젤","2D","디테일","컬러 젤","마무리"],
    body: "① 손·손톱 소독. ② 아트와 조화되는 젤 컬러를 도포한다. ③ 튜브 젤로 케이크 형태를 조형한다. 튜브를 누르는 힘 조절에 유의하고, 세밀한 모양은 브러시로 다듬는다. ④ 형태가 완성되면 젤 램프로 경화한다. ⑤ 여러 컬러 젤로 케이크의 디테일을 디자인한다. ⑥ 조형물 주변까지 톱 젤을 꼼꼼히 도포·경화하여 생활에 불편이 없도록 마무리한다.",
    tips: \["케이크 모형에 먼저 톱 젤을 바르고 그 위에 주변 장식을 표현하면 입체감의 층이 생겨 완성도가 올라간다."]
  },
  {
    id: "n-07", title: "젤 3D 케이크 — 조형", page: "p.44\~45",
    keywords: \["3D","케이크","스패출러","엠보젤","1차 조형","2차 조형","보관"],
    body: "① 손·손톱 소독. ② 스패출러로 젤을 덜어 볼을 만든다. ③ 엠보 젤과 젤 전용 브러시로 3D 모형을 조형한다. 젤 클렌저로 중간중간 브러시를 닦으며 작업하면 편하다. ④ 1차 조형한 모형은 상황에 따라 중간 경화를 하거나, 생략하고 바로 2차 조형으로 넘어간다. ⑤ 완성되면 젤 램프로 경화하고, 필요할 때 쓸 수 있도록 잘 보관한다."
  },
  {
    id: "n-08", title: "젤 3D 모형 접착 및 마무리", page: "p.46\~47",
    keywords: \["접착","핀셋","접착제","톱 젤","논 와이프","백탁","마무리","지속성"],
    body: "① 전체 디자인을 고려해 부착 위치를 정하고 네일 접착제를 도포한다. ② 핀셋으로 모형을 집어 올리고, 필요하면 실리콘 툴이나 오렌지 우드스틱으로 위치를 조정한다. ③ 모형이 틀어지지 않게 정확히 맞춘 뒤 고정한다. ④ 3D 모형은 미경화 젤을 닦아내기 어려우므로 논 와이프 톱 젤을 도포·경화하여 마무리한다.",
    tips: \[
      "틈새 도포는 세필 브러시를 사용한다.",
      "접착제가 모형 밖으로 넘치면 하얀 백탁이 생길 수 있으니 주의한다.",
      "젤 클렌저로 브러시를 페이퍼에 닦아 가며 조형하면 형태가 뚜렷해진다."
    ]
  },
  {
    id: "n-09", title: "안전·위생 종합 체크리스트", page: "p.31,34,38,43",
    keywords: \["안전","위생","젤램프","환기","마스크","응급","클렌저"],
    body: "실습 전후 기구와 재료를 위생적으로 관리한다. 젤 램프 사용 시 광원을 직접 보지 않고 설명서의 사용 시간을 지킨다. 미경화 젤은 젤 클렌저로 닦아낸다. 작업 중에는 마스크를 착용하고 항상 환기에 유의한다. 입체 장식이 과도하게 두껍거나 손톱 끝에 몰려 생활에 불편을 주지 않도록 디자인 단계에서 점검한다. 응급 처치 용품 세트를 비치하고 응급 상황 대비 훈련을 한다."
  }
];
```

\---

# 부록 B. 수학 지식베이스 시드 (`lib/knowledge/math.ts`)

> 2022 개정 교육과정 \*\*공통수학1 「경우의 수」\*\* 영역 기준.
> 성취기준: \*\*\[10공수1-03-01]\*\* 합의 법칙과 곱의 법칙을 이해하고, 적절한 전략을 사용하여 경우의 수와 관련된 문제를 해결할 수 있다. / 같은 영역에서 순열과 조합의 의미를 이해하고 그 수를 구한다.
> (연계) \*\*\[직무수학 149-1-01]\*\* 실제 직무 상황에서 두 가지 이상의 조건을 고려해 경우의 수를 구할 수 있다.

```ts
export const MATH\_CHUNKS: Chunk\[] = \[
  {
    id: "m-01", title: "합의 법칙", keywords: \["합","합의 법칙","또는","동시에","배타"],
    body: "두 사건 A, B가 동시에 일어나지 않을 때, A가 일어나는 경우가 m가지, B가 일어나는 경우가 n가지이면 A 또는 B가 일어나는 경우의 수는 m+n이다. 핵심 신호어는 '또는'이다. 겹치는 경우가 있으면 겹치는 만큼 빼야 한다.",
    nailExample: "따뜻한 팔레트 3종 또는 차가운 팔레트 4종 중 하나를 고른다면 3+4=7가지."
  },
  {
    id: "m-02", title: "곱의 법칙", keywords: \["곱","곱의 법칙","그리고","단계","연속"],
    body: "사건 A가 일어나는 경우가 m가지이고, 그 각각에 대해 사건 B가 일어나는 경우가 n가지이면, A와 B가 잇따라 일어나는 경우의 수는 m×n이다. 핵심 신호어는 '그리고', '이어서'이다.",
    nailExample: "베이스 컬러 5가지 × 아트 기법 4가지 × 입체 모티브 3가지 = 60가지 팁 디자인."
  },
  {
    id: "m-03", title: "순열 nPr", keywords: \["순열","P","배열","배치","나열","순서"],
    body: "서로 다른 n개에서 r개를 뽑아 순서를 정해 나열하는 경우의 수를 nPr로 나타내고, nPr = n(n-1)(n-2)…(n-r+1) = n!/(n-r)! 이다. (0 ≤ r ≤ n, 0! = 1, nP0 = 1, nPn = n!)",
    nailExample: "10개 팁 자리 중 시그니처 팁 3개의 위치를 순서까지 정하면 10P3 = 720가지."
  },
  {
    id: "m-04", title: "조합 nCr", keywords: \["조합","C","선택","뽑기","순서 무관","고르기"],
    body: "서로 다른 n개에서 순서를 생각하지 않고 r개를 뽑는 경우의 수를 nCr로 나타내고, nCr = nPr / r! = n! / (r!(n-r)!) 이다. nCr = nC(n-r), nC0 = nCn = 1 이 성립한다.",
    nailExample: "후보 모티브 8개 중 5개를 고르면 8C5 = 56가지."
  },
  {
    id: "m-05", title: "순열과 조합 구분법 (가장 많이 틀리는 지점)",
    keywords: \["차이","구분","순열 조합","헷갈","언제"],
    body: "뽑은 뒤 '자리·순서·배치'가 결과를 바꾸면 순열, 뽑기만 하고 순서가 결과를 바꾸지 않으면 조합이다. 판별 질문: '두 개를 서로 바꿔 놓으면 다른 결과인가?' 다르면 순열(P), 같으면 조합(C). 항상 nCr ≤ nPr 이며 nPr = nCr × r! 이다.",
    nailExample: "10팁 중 5개 모티브를 '고르기'만 하면 조합, 그 5개를 어느 손가락에 '배치'할지까지 정하면 순열."
  },
  {
    id: "m-06", title: "조건이 붙는 경우 (여사건·제외)",
    keywords: \["조건","제외","빼기","여사건","제약","돌발"],
    body: "특정 요소를 반드시 제외해야 하면 후보 수 자체를 줄여서 다시 계산하고, '적어도 하나'처럼 조건이 복잡하면 (전체) − (조건을 만족하지 않는 경우)로 구하는 것이 편하다.",
    nailExample: "모델이 특정 컬러 1개를 제외해 달라고 하면 베이스 컬러가 5→4가 되어 5×4×3=60이 4×4×3=48로 줄어든다."
  }
];
```

\---

# 부록 C. 학습지도안 요약 \& 추천 질문 (`lib/knowledge/lesson.ts`)

## C-1. 수업 메타

```ts
export const LESSON = {
  title: "화가의 작품을 담은 메이크업쇼 네일아트",
  subtitle: "경우의 수를 활용한 젤 2D 입체 네일아트 10팁 컬렉션 기획·제작",
  subject: "네일 미용(전공) × 수학(경우의 수)",
  target: "미디어메이크업과 2학년 1반 (5개 조)",
  teacher: "박기연",
  sessions: \[
    { n: 1, title: "개념 이해 · 화가 분석 마인드맵 · AI 디자인 초안",
      activities: \["입체 네일아트 개념(2D/3D, 젤 특성, 경화 원리)",
                   "팀별 화가 분석 마인드맵(색감·선·질감·분위기·소재)",
                   "AI 디자인 아이디어 생성, 요소별 선택지 정리, 경우의 수 산출"] },
    { n: 2, title: "★연구수업(본시) — 디자인 확정 · 젤 2D 조형·경화 실습",
      activities: \["AI 초안 분석·수정(제작 가능성·두께·위치·색 조화·통일감)",
                   "조별 협의로 디자인 확정, 개인별 담당 팁 계획",
                   "교사 시범 후 개인별 담당 팁 조형·경화(21분)"] },
    { n: 3, title: "톱 젤 마무리 · 조별 발표 · 성찰",
      activities: \["톱 젤 도포·경화로 광택·지속성 마감, 10팁 배열·전시",
                   "조별 발표(재해석 스토리 + 경우의 수 근거 + AI 수정 과정, 2분)",
                   "동료 피드백·성찰(AI가 한 일 / 내가 한 일 구분)"] }
  ],
  rubricSummary: {
    상: "AI 제안 디자인을 창의적으로 수정해 통일감 있는 10팁 컬렉션을 조형·경화·마감까지 완성하고, 경우의 수 원리를 정확히 적용해 조합을 산출·설명하며 근거를 논리적으로 발표한다.",
    중: "경우의 수로 조합 수를 구해 의미를 설명하고, AI 제안을 부분 수정해 담당 팁을 조형·경화하며 조별 활동에 기여한다.",
    하: "교사·동료·웹앱의 도움을 받아 경우의 수를 제한적으로 적용하고 담당 팁 제작을 시도한다."
  }
};
```

## C-2. 추천 질문 목록 (`SUGGESTED\_QUESTIONS`)

|차시|카테고리|질문|
|-|-|-|
|1|전공|2D 입체 네일아트와 3D 입체 네일아트는 뭐가 다른가요?|
|1|전공|젤은 어떤 특성이 있어서 입체 조형이 가능한가요?|
|1|전공|젤 램프로 경화하는 원리가 뭔가요?|
|1|전공|무대 조명 아래에서 2D 입체 장식이 주는 효과는 뭔가요?|
|1|경우의수|베이스 컬러 5, 아트 기법 4, 입체 모티브 3이면 팁 디자인은 몇 가지예요?|
|1|경우의수|왜 더하는 게 아니라 곱하나요?|
|1|경우의수|후보 모티브 8개 중 5개를 고르면 몇 가지인가요?|
|1|수업진행|1차시에 우리 조가 해야 할 일이 뭔가요?|
|1|수업진행|마인드맵에 무엇을 분석해서 적어야 하나요?|
|2|전공|이 두께로 조형해도 경화가 잘 될까요?|
|2|전공|젤이 자꾸 퍼지는데 어떻게 하면 형태가 잡히나요?|
|2|전공|입체 장식 위에 톱 젤은 어떻게 발라야 하나요?|
|2|전공|미경화 젤은 어떻게 처리하나요?|
|2|전공|튜브 젤로 조형할 때 주의할 점은 뭔가요?|
|2|전공|3D 모형은 어떤 접착제로 어떻게 붙이나요?|
|2|전공|젤 램프 쓸 때 안전 수칙을 알려주세요.|
|2|경우의수|후보 중 5개를 고르는 조합 수를 계산해 주세요.|
|2|경우의수|컬러 하나를 빼면 경우의 수가 어떻게 달라지나요?|
|2|수업진행|디자인을 확정할 때 무엇을 기준으로 판단해야 하나요?|
|2|수업진행|활동지에 무엇을 기록해야 하나요?|
|3|전공|톱 젤로 마무리할 때 입체감이 사라지지 않게 하려면?|
|3|전공|10팁을 하나의 컬렉션으로 보이게 배열하려면 어떻게 하나요?|
|3|경우의수|순서가 중요하지 않으면 순열 대신 무엇을 써야 하나요?|
|3|경우의수|10P3과 10C3은 얼마나 차이가 나나요?|
|3|수업진행|발표는 어떤 순서로 구성해야 하나요?|
|3|수업진행|평가에서 '상'을 받으려면 무엇이 필요한가요?|
|공통|디자인|제 디자인 사진을 봐주세요. (→ 업로더 열기)|
|공통|디자인|우리 조 화가에 맞는 샘플 디자인을 보여주세요.|

\---

# 부록 D. 조별 화가 배정 \& 디자인 팔레트 (`lib/knowledge/artists.ts`)

> `mood` 문자열은 \*\*이미지 생성 프롬프트에 그대로 들어가는 요소 형용사\*\*다. 작품 제목·작가명은 넣지 않는다.

```ts
export const GROUPS = \[
  {
    group: 1, artist: "빈센트 반 고흐", accentHex: "#1E4B8F",
    mood: "swirling brushstroke texture, deep cobalt blue and golden yellow contrast, thick impasto ridges",
    baseColors: \["딥 코발트 블루","머스터드 옐로우","다크 네이비","아이보리","올리브 그린"],
    techniques: \["엠보 젤 라인 스트로크","그러데이션","글리터 믹스","마블링"],
    motifs: \["소용돌이 라인","별 모티브","사이프러스 실루엣"],
    countingExample: "곱의 법칙 5×4×3 = 60가지"
  },
  {
    group: 2, artist: "클로드 모네", accentHex: "#6FA8B8",
    mood: "soft pastel color blending, hazy watercolor edges, pale lavender pink and misty blue-green",
    baseColors: \["연보라","파스텔 핑크","미스티 블루","세이지 그린","크림 화이트"],
    techniques: \["워터 그러데이션","블러 엠보","펄 톱 젤","도트 텍스처"],
    motifs: \["수련 꽃잎","물결 라인","작은 잎사귀"],
    countingExample: "합의 법칙 — 따뜻한 팔레트 또는 차가운 팔레트 선택"
  },
  {
    group: 3, artist: "구스타프 클림트", accentHex: "#C9A227",
    mood: "ornate gold leaf texture, geometric spirals and mosaic tiles, warm amber and black contrast",
    baseColors: \["골드","블랙","앰버","버건디","샴페인 베이지"],
    techniques: \["골드 포일","엠보 기하 패턴","스톤 세팅","라인 드로잉"],
    motifs: \["나선 문양","사각 모자이크","눈물방울"],
    countingExample: "순열 10P3 = 720가지 (시그니처 팁 배치)"
  },
  {
    group: 4, artist: "피에트 몬드리안", accentHex: "#D93A2B",
    mood: "clean primary red blue yellow blocks separated by bold black grid lines on white",
    baseColors: \["화이트","프라이머리 레드","프라이머리 블루","프라이머리 옐로우","블랙"],
    techniques: \["블록 컬러링","엠보 라인 그리드","매트 톱 젤","면 분할"],
    motifs: \["사각 블록","교차 라인","코너 포인트"],
    countingExample: "조합 8C5 = 56가지"
  },
  {
    group: 5, artist: "쿠사마 야요이", accentHex: "#E0447C",
    // ⚠️ 생존 작가 — 이미지 프롬프트에 작가명·작품명을 넣지 말 것. mood 형용사만 사용.
    mood: "repeating polka dot pattern in high-contrast primary colors, rhythmic scattered round dots",
    baseColors: \["비비드 레드","블랙","화이트","비비드 옐로우","핫 핑크"],
    techniques: \["도트 반복","크기 변주 도트","엠보 볼 조형","컬러 대비"],
    motifs: \["원형 도트","호박 실루엣","반복 패턴 그리드"],
    countingExample: "순열 6P4 = 360가지 (포인트 배열)"
  }
];
```

### D-1. 조별 샘플 디자인 3안 (텍스트 명세 — F-07용)

각 조마다 아래 형식으로 **3안**을 사전 작성한다. (Cursor가 위 팔레트를 조합해 생성하되, 형식은 고정)

```ts
{
  id: "g3-a", group: 3, name: "골드 모자이크 미니멀",
  concept: "10팁 중 3팁만 골드 나선 포인트, 나머지는 블랙 무광으로 절제",
  base: "블랙", technique: "엠보 라인 그리드", motif: "나선 문양",
  tipPlan: "1·5·10번 팁에 시그니처 나선, 나머지는 단색 + 코너 도트",
  countingBasis: "시그니처 팁 3개 위치를 정하는 경우 10P3 = 720가지 중, 좌우 균형 조건으로 3안 압축",
  makeSteps: \["소독", "블랙 컬러 젤 도포·경화", "엠보 젤로 나선 조형", "부분 경화 후 형태 고정", "골드 포일 포인트", "톱 젤 마무리"],
  cautions: \["나선 두께가 1mm를 넘지 않게", "장식이 프리에지 쪽으로 몰리지 않게"]
}
```

\---

## 12\. README에 반드시 넣을 문구 (키 교체 안내)

```md
### API 키 교체 방법
1. `.env.local` 파일을 연다.
2. `OPENAI\_API\_KEY=sk-proj-DUMMY-...` 줄을 실제 키로 바꾼다.
3. `MOCK\_MODE=false` 로 바꾼다.
4. 개발 서버를 재시작한다. (`Ctrl+C` 후 `npm run dev`)
※ Vercel 배포 시에는 Settings → Environment Variables에 동일한 이름으로 등록한다.
※ 키는 절대 GitHub에 올리지 않는다. `.env.local`은 `.gitignore`에 포함되어 있다.
```

## 13\. 출처 표기 (앱 푸터 · README)

* 교육부(2025). NCS학습모듈 「입체 네일아트」(LM1201010455\_23v4). 한국직업능력연구원. — 수업 활용 목적 인용
* 교육부(2022). 2022 개정 수학과 교육과정 — 공통수학1 「경우의 수」
* 2026학년도 전공(네일 미용)·수학 융합 교수학습과정안 (지도교사 박기연)


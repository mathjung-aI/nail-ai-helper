[build]
  command = "npm run build"
  publish = ".next"

[build.environment]
  NODE_VERSION = "20"

[functions]
  # 이미지 생성(edit/generate)은 수십 초 소요될 수 있음
  node_bundler = "esbuild"

[[plugins]]
  package = "@netlify/plugin-nextjs"
